name: flutter_conditonal_example
description: "An example project for flutter_conditional"
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_conditional: ^1.0.0+1

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true